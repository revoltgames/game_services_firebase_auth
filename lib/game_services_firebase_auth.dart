import 'game_services_firebase_auth_platform_interface.dart';

/// The entry point of the Game Services Firebase Auth SDK.
class GameServicesFirebaseAuth {
  static GameServicesFirebaseAuthPlatform get instance =>
      GameServicesFirebaseAuthPlatform.instance;
}
